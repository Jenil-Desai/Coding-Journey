enum Direction {
    North,
    South,
    East,
    West,
}

fn main() {
    let my_direction = Direction::North;
    move_direction(my_direction)
}

fn move_direction(direction: Direction) {
    // Direction Moves
}
