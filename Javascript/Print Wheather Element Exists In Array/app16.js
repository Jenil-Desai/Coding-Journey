let str = ['Jenil', 'Desai', 18, 2.2];
let item = prompt("Enter Item : ");

if (str.indexOf(item)!=-1) {
    alert("Item is avaliable in array.");
} else {
    alert("Item is not avaliable in array.");
}